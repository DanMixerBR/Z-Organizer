name: Build Z-Organizer

on:
  workflow_dispatch: 
    inputs:
      versao_tag:
        description: 'Tag da Versão (ex: v1.0.0)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: write

jobs:
  build:
    name: Build Executables
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: Z-Organizer.exe
            zip_name: Z-Organizer_Windows.zip
          - os: ubuntu-latest
            artifact_name: Z-Organizer
            zip_name: Z-Organizer_Linux.zip

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install nuitka pefile

      - name: Install System Dependencies (Linux Only)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc python3-dev zip patchelf

      # --- Compilação Windows (Nuitka + Warp Packer + RCEdit + PEFile) ---
      - name: Build Windows Executable
        if: matrix.os == 'windows-latest'
        run: |
          # 1. Nuitka gera o binário com o nome correto dentro da pasta standalone
          python -m nuitka --assume-yes-for-downloads --standalone --disable-ccache --windows-console-mode=disable --windows-icon-from-ico="assets/icon.ico" --enable-plugin=tk-inter --output-filename=Z-Organizer.exe main.py
          
          # 2. Warp Packer cria o envelope Onefile
          Invoke-WebRequest -Uri https://github.com/dgiagio/warp/releases/download/v0.3.0/windows-x64.warp-packer.exe -OutFile warp-packer.exe
          .\warp-packer.exe --arch windows-x64 --input_dir main.dist --exec Z-Organizer.exe --output Z-Organizer.exe

          # 3. RCEdit injeta o ícone e metadados no envelope do Warp
          Invoke-WebRequest -Uri https://github.com/electron/rcedit/releases/download/v2.0.0/rcedit-x64.exe -OutFile rcedit.exe
          .\rcedit.exe Z-Organizer.exe --set-icon "assets/icon.ico" --set-version-string "CompanyName" "DanMixerBR" --set-version-string "ProductName" "Z-Organizer" --set-version-string "FileDescription" "Gerenciamento Inteligente de Arquivos" --set-version-string "FileVersion" "${{ github.event.inputs.versao_tag }}"
          
          # 4. PEFile remove o terminal do envelope do Warp (Subsystem 3 -> 2)
          python -c "import pefile; pe = pefile.PE('Z-Organizer.exe'); pe.OPTIONAL_HEADER.Subsystem = 2; pe.write('Z-Organizer-GUI.exe'); pe.close()"
          Move-Item -Path Z-Organizer-GUI.exe -Destination Z-Organizer.exe -Force

      # --- Compilação Linux (Nuitka + Warp Packer Corrigido) ---
      - name: Build Linux Executable
        if: matrix.os == 'ubuntu-latest'
        run: |
          # 1. Nuitka gera o binário com o nome fixo
          python -m nuitka --assume-yes-for-downloads --standalone --disable-ccache --enable-plugin=tk-inter -o Z-Organizer main.py
          
          # 2. Warp Packer cria o envelope Onefile para Linux
          wget https://github.com/dgiagio/warp/releases/download/v0.3.0/linux-x64.warp-packer -O warp-packer
          chmod +x warp-packer
          ./warp-packer --arch linux-x64 --input_dir main.dist --exec Z-Organizer --output Z-Organizer

      - name: Criar version.txt e preparar pasta bin
        shell: bash
        run: |
          echo "${{ github.event.inputs.versao_tag }}" | tr -d 'A-Za-z' > version.txt
          mkdir -p bin
          cp version.txt bin/
          cp assets/logo.png bin/

      - name: Zip Artifact (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          cp assets/icon.ico bin/
          7z a -tzip -mx=9 ${{ matrix.zip_name }} Z-Organizer.exe bin

      - name: Zip Artifact (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          cp assets/icon.png bin/
          cp assets/Install_Z-Organizer.sh ./
          chmod +x Install_Z-Organizer.sh
          zip -9 -r ${{ matrix.zip_name }} Z-Organizer Install_Z-Organizer.sh bin

      - name: Upload Artifacts to Pipeline
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.zip_name }}
          path: ${{ matrix.zip_name }}

  release-and-publish:
    name: Generate Hashes and Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write 
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./downloads
          merge-multiple: true

      - name: Calculate Hashes and Update hash.txt
        run: |
          HASH_WIN=$(sha256sum ./downloads/Z-Organizer_Windows.zip | awk '{ print $1 }')
          HASH_LINUX=$(sha256sum ./downloads/Z-Organizer_Linux.zip | awk '{ print $1 }')
          echo "$HASH_WIN" > hash.txt
          echo "$HASH_LINUX" >> hash.txt

      - name: Salvar os Hashes direto no Repositório (Commit Automático)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 Atualização automática de hashes para a versão ${{ github.event.inputs.versao_tag }}"
          file_pattern: "hash.txt"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.versao_tag }}
          name: "Z-Organizer ${{ github.event.inputs.versao_tag }}"
          files: |
            ./downloads/Z-Organizer_Windows.zip
            ./downloads/Z-Organizer_Linux.zip
          draft: true
